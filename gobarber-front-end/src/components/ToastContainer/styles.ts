import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden; // Tudo que estiver mostrando além do tamanho do container irá ficar invisível
`;
